import tkinter as tk
# from ui.commands import *


class Log(tk.Toplevel):

    def __init__(self):
        # tk.Tk.__init__(self)
        # self.iconify()
        # self.root = root
        self.log_ui()

    def log_ui(self):
        usr = tk.Entry(self)
        usr.grid(row=0, column=1)
        psw = tk.Entry(self)
        psw.grid(row=1, column=1)
        psw.config(show="*")
        tk.Label(self, text='username: ', height=2).grid(column=0, row=0)
        tk.Label(self, text='password: ', height=2).grid(column=0, row=1)
        tk.Button(self, text="enter", command=self.log_response).grid(column=1, row=2)

    def log_response(self):
        print('accesso in corso')
        return 1
        # found = 1
        # if found:
            # return True
        # NewRoot = tk.Toplevel(root)
        # tk.Toplevel(self.root)

# command=self.login()
# def login(self):